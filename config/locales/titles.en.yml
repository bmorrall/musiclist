en:
  titles:
    # titles.application defaults to the sigficant portion of
    # Musiclist::Application, which would be:
    application: Musiclist
    artists:
      index: "Artists | Musiclist"
      show: '%{artist} | Musiclist'
      edit: 'Editing %{artist} | Musiclist'
    albums:
      index: "Albums | Musiclist"
      show: '%{album} | Musiclist'
      edit: 'Editing %{album} | Musiclist'
      refreshes:
        show: "Refreshing %{album} | Musiclist"
      years:
        index: "Albums by Year | Musiclist"
        show: "Albums from %{year} | Musiclist"
